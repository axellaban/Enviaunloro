node_modules
.next
.env.local
.data
*.tsbuildinfo
next-env.d.ts

# Los escribe "next dev" solo, cada vez que arranca: instrucciones en inglés
# para agentes, generadas por Next y no por este proyecto.
AGENTS.md
CLAUDE.md

# El directorio temporal que crea scripts/miniatura.mjs para compilar las aves.
.aves-*
